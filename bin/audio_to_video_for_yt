#!/usr/bin/fish

echo image: $argv[1]
echo audio: $argv[2]
echo video: $argv[3]

#
# TODO: Deduce video file name from audio (audio-basename + mp4)
#

ffmpeg -loop 1 -i $argv[1]  -i $argv[2] -c:a copy -c:v libx264 -shortest $argv[3]

