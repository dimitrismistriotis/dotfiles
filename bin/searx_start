#!/bin/bash
#
# https://searx.github.io/searx/admin/installation-docker.html#installation-docker
#
CONTAINER_NAME="local_searx"
PORT=8085
docker pull searx/searx
docker run --name ${CONTAINER_NAME} --rm -d -v ${PWD}/searx:/etc/searx -p $PORT:8080 -e BASE_URL=http://localhost:$PORT/ searx/searx
